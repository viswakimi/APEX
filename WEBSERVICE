  CREATE TABLE "EMP" 
   (	"EMPNO" NUMBER(4,0) GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9000000 INCREMENT BY 10 START WITH 8000 NOCACHE  NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ENAME" VARCHAR2(50), 
	"JOB" VARCHAR2(50), 
	"MGR" NUMBER(4,0), 
	"HIREDATE" DATE, 
	"SAL" NUMBER(7,2), 
	"COMM" NUMBER(7,2), 
	"DEPTNO" NUMBER(4,0), 
	"IMAGE" BLOB, 
	 CONSTRAINT "EMP_PK" PRIMARY KEY ("EMPNO")
  USING INDEX  ENABLE
   ) ;
   
   -------------------------- apex page code ----
   
   DECLARE
    l_response      CLOB;

BEGIN
    -- 1. Call the REST API
    l_response := APEX_WEB_SERVICE.MAKE_REST_REQUEST(
        p_url         => 'http://localhost:9090/ords/epm/emp/info', -- change end point as per your web service
        p_http_method => 'GET'
    );

    -- 2. Create the collection (if not already created)
    APEX_COLLECTION.CREATE_OR_TRUNCATE_COLLECTION('REST_COLLECTION');

        APEX_COLLECTION.ADD_MEMBER(
            p_collection_name => 'REST_COLLECTION',
            p_clob001 => l_response
        );
END;




-
-- Create OAUTH client
BEGIN
  OAUTH.create_client(
    p_name            => 'X API Client',
    p_grant_type      => 'client_credentials',
    p_owner           => 'aXXXX',
    p_description     => 'A client for Rest Demo',
    p_support_email   => 'XXX@gmail.com',
    p_privilege_names => 'X_api-priv'
  );
  -- Grant OAUTH client role
  OAUTH.grant_client_role(
    p_client_name => 'X API Client',
    p_role_name   => 'X-api-role'
  );

  COMMIT;
END;


SELECT id, name, client_id, client_secret
FROM   user_ords_clients;
