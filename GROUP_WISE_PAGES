GROUP_WISE_PAGES

CREATE TABLE "GROUP_WISE_PAGES" 
   (	"WORKSPACE" VARCHAR2(500), 
	"APP_PAGE_ID" NUMBER NOT NULL ENABLE, 
	"APP_ID" NUMBER NOT NULL ENABLE, 
	"GROUP_ID" VARCHAR2(10) NOT NULL ENABLE, 
	"INSERT_ALLOWED" CHAR(1), 
	"UPDATE_ALLOWED" CHAR(1), 
	"DELETE_ALLOWED" CHAR(1), 
	"QUERY_ALLOWED" CHAR(1), 
	"CREATE_BY" VARCHAR2(100), 
	"CREATED_ON" DATE, 
	"UPDATED_BY" VARCHAR2(30), 
	"UPDATED_ON" DATE, 
	"WORKSPACE_ID" NUMBER, 
	"APP_NAME" VARCHAR2(1000), 
	"PAGE_NAME" VARCHAR2(500), 
	 CONSTRAINT "GROUP_WISE_PAGES_PK" PRIMARY KEY ("APP_PAGE_ID", "APP_ID", "GROUP_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "GROUP_WISE_PAGES" ADD CONSTRAINT "GROUP_WISE_PAGES_FK" FOREIGN KEY ("GROUP_ID")
	  REFERENCES "GROUPS" ("GROUP_ID") ENABLE;


-- just replace GROUP_WISE_PAGES table with your table name and select the columns

select null,APP_PAGE_ID, 'f?p=&APP_ID.:'||APP_PAGE_ID||':&APP_SESSION.::NO::' target
from GROUP_WISE_PAGES order by app_page_id


--LINK LIST
.t-LinksList-item:hover{
background-color: rgb(249, 245, 189);
}
